import React, { useState } from 'react';

const Grid = props => {

  const cardInfo = [

  ]

  const [grid, setGrid] = useState(Array(9).fill(null))
  
  const populateGrid = () => {
    
  }

}

export default Grid;